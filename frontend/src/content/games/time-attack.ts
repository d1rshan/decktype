import type { GameDefinition } from '../../entities/game/types'

const timeAttackGame: GameDefinition = {
  id: 'time-attack',
  slug: 'time-attack',
  name: 'Time Attack',
  description: 'A short sprint mode tuned for raw speed and rapid resets.',
  defaultLanguage: 'en',
  status: 'coming-soon',
}

export default timeAttackGame
