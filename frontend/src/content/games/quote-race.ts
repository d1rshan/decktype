import type { GameDefinition } from '../../entities/game/types'

const quoteRaceGame: GameDefinition = {
  id: 'quote-race',
  slug: 'quote-race',
  name: 'Quote Race',
  description: 'Long-form passages, higher accuracy pressure, calmer pacing.',
  defaultLanguage: 'en',
  status: 'coming-soon',
}

export default quoteRaceGame
